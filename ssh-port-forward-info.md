# 🔄 SSH 端口转发配置

## 配置信息
- **公网端口：** 36325
- **目标端口：** 22 (SSH)
- **入口IP：** 216.167.28.3
- **服务器IP：** 10.2.3.1 (内部)
- **状态：** ✅ 已启用

## 连接方式

### 通过端口转发连接
```bash
ssh root@216.167.28.3 -p 36325
```

### 测试连接
```bash
# 检查端口是否开放
nc -zv 216.167.28.3 36325

# 测试 SSH 连接
ssh -o ConnectTimeout=5 root@216.167.28.3 -p 36325 "echo '连接成功!'"
```

## 防火墙规则
```bash
# 查看端口转发规则
/usr/sbin/iptables -t nat -L -n

# 保存规则
/usr/sbin/iptables-save > /etc/iptables/rules.v4

# 重启后恢复规则
systemctl restart netfilter-persistent
```

## 管理命令
```bash
# 添加端口转发
/usr/sbin/iptables -t nat -A PREROUTING -p tcp --dport 36325 -j REDIRECT --to-port 22

# 删除端口转发
/usr/sbin/iptables -t nat -D PREROUTING -p tcp --dport 36325 -j REDIRECT --to-port 22
```

## 安全注意事项
- 端口 36325 已暴露到公网
- 建议使用强密码或密钥认证
- 监控 SSH 登录尝试
- 考虑使用 fail2ban 保护

---
*配置时间：2026-02-24*